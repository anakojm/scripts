sh -c "$(echo -e 'lock\nloginctl poweroff\nloginctl reboot\nswaymsg exit'|fzf --reverse --preview="date '+%D %H:%M:%S';acpi | sed 's/Battery 0://;s/until charged//;s/remaining//'" --preview-window=up:wrap:noborder)"
